version: 0.2
phases:
  install:
    runtime-versions:
      java: corretto8
  build:
    commands:
    - echo Running tests 
    - mvn test
    - mvn clean package
    
reports:
  php-reports:
    files:
      - "report.json"
    file-format: "JUNITXML"
